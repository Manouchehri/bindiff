package com.google.common.hash;

import com.google.common.hash.HashCode;
import javax.annotation.Nullable;

interface CityHash128HashFunction$InternalImplementation {
   HashCode fingerprint(byte[] var1, int var2, int var3, @Nullable long[] var4);
}
