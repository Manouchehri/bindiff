package org.jfree.base.modules;

import org.jfree.base.config.*;

public class PackageManager$PackageConfiguration extends PropertyFileConfiguration
{
    public void insertConfiguration(final HierarchicalConfiguration hierarchicalConfiguration) {
        super.insertConfiguration(hierarchicalConfiguration);
    }
}
