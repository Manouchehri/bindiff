package y.f.e;

import y.f.b.*;
import y.c.*;
import java.awt.geom.*;
import y.f.*;

class v implements a
{
    private final q a;
    
    v(final q a) {
        this.a = a;
    }
    
    public Rectangle2D a(final X x, final y.c.q q, final y y) {
        final am h = x.h(q);
        return new Rectangle2D.Double(h.getX(), h.getY(), h.getWidth(), h.getHeight());
    }
}
