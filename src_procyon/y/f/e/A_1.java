package y.f.e;

import y.f.b.*;
import y.f.*;
import y.c.*;
import java.awt.geom.*;

final class A implements a
{
    private final a a;
    
    public A(final a a) {
        this.a = a;
    }
    
    public Rectangle2D a(final X x, final q q, final y y) {
        if (y.isEmpty()) {
            final Rectangle2D a = this.a.a(x, q, y);
            a.setFrame(x.j(q) - a.getWidth() * 0.5, x.k(q) - a.getHeight() * 0.5, a.getWidth(), a.getHeight());
            return a;
        }
        return this.a.a(x, q, y);
    }
}
