package com.google.protobuf;

public enum Extension$ExtensionType
{
    IMMUTABLE("IMMUTABLE", 0), 
    MUTABLE("MUTABLE", 1), 
    PROTO1("PROTO1", 2);
    
    private Extension$ExtensionType(final String s, final int n) {
    }
}
