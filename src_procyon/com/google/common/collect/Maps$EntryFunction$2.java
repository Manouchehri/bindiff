package com.google.common.collect;

import java.util.*;
import javax.annotation.*;

enum Maps$EntryFunction$2
{
    Maps$EntryFunction$2(final String s, final int n) {
    }
    
    @Nullable
    public Object apply(final Map.Entry entry) {
        return entry.getValue();
    }
}
