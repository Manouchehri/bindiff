package com.google.common.collect;

import com.google.common.base.*;

enum Maps$EntryFunction implements Function
{
    KEY("KEY", 0), 
    VALUE("VALUE", 1);
    
    private Maps$EntryFunction(final String s, final int n) {
    }
}
