package com.google.common.hash;

import javax.annotation.*;

interface CityHash128HashFunction$InternalImplementation
{
    HashCode fingerprint(final byte[] p0, final int p1, final int p2, @Nullable final long[] p3);
}
