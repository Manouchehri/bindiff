package com.google.security.zynamics.bindiff.graph.filter.enums;

public enum ESelectionFilter
{
    NONE("NONE", 0), 
    SELECTED("SELECTED", 1), 
    UNSELECTED("UNSELECTED", 2);
    
    private ESelectionFilter(final String s, final int n) {
    }
}
