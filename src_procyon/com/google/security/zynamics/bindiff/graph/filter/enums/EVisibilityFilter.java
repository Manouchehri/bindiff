package com.google.security.zynamics.bindiff.graph.filter.enums;

public enum EVisibilityFilter
{
    NONE("NONE", 0), 
    VISIBLE("VISIBLE", 1), 
    INVISIBLE("INVISIBLE", 2);
    
    private EVisibilityFilter(final String s, final int n) {
    }
}
