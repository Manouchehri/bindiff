package com.google.security.zynamics.bindiff.enums;

public enum EGraphSynchronization
{
    SYNC("SYNC", 0), 
    ASYNC("ASYNC", 1);
    
    private EGraphSynchronization(final String s, final int n) {
    }
}
