package com.google.security.zynamics.bindiff.enums;

public enum EPlacement
{
    LEFT("LEFT", 0), 
    TOP("TOP", 1), 
    RIGHT("RIGHT", 2), 
    BOTTOM("BOTTOM", 3);
    
    private EPlacement(final String s, final int n) {
    }
}
