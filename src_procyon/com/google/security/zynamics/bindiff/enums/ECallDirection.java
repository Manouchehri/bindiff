package com.google.security.zynamics.bindiff.enums;

public enum ECallDirection
{
    INCOMING("INCOMING", 0), 
    OUTGOING("OUTGOING", 1);
    
    private ECallDirection(final String s, final int n) {
    }
}
