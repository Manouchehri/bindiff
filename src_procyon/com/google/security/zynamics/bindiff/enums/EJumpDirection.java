package com.google.security.zynamics.bindiff.enums;

public enum EJumpDirection
{
    INCOMING("INCOMING", 0), 
    OUTGOING("OUTGOING", 1);
    
    private EJumpDirection(final String s, final int n) {
    }
}
