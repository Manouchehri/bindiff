package com.google.security.zynamics.bindiff.enums;

public enum EDiffViewMode
{
    NORMAL_VIEW("NORMAL_VIEW", 0), 
    COMBINED_VIEW("COMBINED_VIEW", 1), 
    TEXT_VIEW("TEXT_VIEW", 2);
    
    private EDiffViewMode(final String s, final int n) {
    }
}
