package com.google.security.zynamics.bindiff.enums;

public enum ESortOrder
{
    ASCENDING("ASCENDING", 0), 
    DESCENDING("DESCENDING", 1);
    
    private ESortOrder(final String s, final int n) {
    }
}
