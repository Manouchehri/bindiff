package com.google.security.zynamics.zylib.types.common;

public enum IterationMode
{
    CONTINUE("CONTINUE", 0), 
    STOP("STOP", 1);
    
    private IterationMode(final String s, final int n) {
    }
}
